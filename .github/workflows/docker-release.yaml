name: Docker Image CI

on:
  push:
    branches: [ docker ]
  pull_request:
    branches: [ docker ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        ref: docker
    - name: Build the Docker image 
      run: |
        make docker-buildenv
        make docker-runtime
        # docker build . --file dockerfile --tag filvenus/venus:${{steps.vars.outputs.tag}}
        # docker tag filvenus/venus:${{steps.vars.outputs.tag}} filvenus/venus:latest
        docker build . --file dockerfile --tag filvenus/venus:latest
    - name: Login to registry
      run: docker login --username=filvenus --password ${{ secrets.DOCKER_PASSWORD }} 
    - name: Push Image
      run: |
        # docker push filvenus/venus:${{steps.vars.outputs.tag}}
        docker push filvenus/venus:latest